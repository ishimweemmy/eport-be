{{> header}}
    <h1>Welcome to E-Portfolio {{userName}}!</h1>
    <p>We're excited to have you join our community.</p>

    {{#if isNewUser}}
        {{> button url=dashboardUrl text="Get Started"}}
    {{/if}}
{{> footer}}